#pragma once

#include <SkPoint.h>
#include <SkSize.h>

namespace otto::skia {
  using Point = SkPoint;
  using Vector = Point;
  using IPoint = SkIPoint;
  using IVector = IPoint;
} // namespace otto::skia
